# rtkpost_main.c 代码审查与数据验证报告

验证日期：2026-08-19  
RTKLIB：2.4.3 b34  
测试数据：`测试结果/流动站/20260813200000.obs`、`测试结果/基准站/20260813200000.obs`、`测试结果/流动站/20260813200000.nav`

## 发现并修复的问题

1. `-sys` 解析错误：原 `switch` 没有 `break`，指定 `-sys G` 会继续启用后续星座；同时原循环不能正确解析 `GEC` 这种帮助中给出的连续写法。现已支持 `GEC` 和 `G,E,C`，并拒绝未知字符。
2. 默认参数与帮助、现有 RTKPOST 配置不一致：原代码默认 Combined、Fix-and-Hold、双频无电离层组合、多星座，但帮助或现有 `数据.conf` 使用 Forward、Continuous、广播电离层、GPS。现已采用与 RTKLIB 命令行基准一致的 Forward、Continuous、广播电离层以及 GPS+GLONASS 默认星座。
3. 缺少基准站观测插值：现有 `数据.conf` 的 `misc-timeinterp=on`，原驱动未设置，造成相邻历元存在厘米级差异。现已设置 `prcopt.intpref=1`。
4. 参数安全性：新增定位模式、频率数、星座字符和输入文件数量校验；`-s` 改用有长度限制的 `snprintf`，避免缓冲区越界。
5. 退出码注释已核实：RTKLIB `postpos()` 正常完成返回 0，错误返回 -1，用户中止返回 1，驱动直接返回该状态。

## 实测结果

运行参数（与现有 `数据.conf` 的关键参数对齐）：

```text
rtkpost_main.exe -p 2 -f 1 -sys G -iono 1 -m 10 -t -d 3 rover.obs base.obs rover.nav -o validation_fixed.pos
```

| 项目 | 现有 RTKPOST 结果 | 修复后的程序 |
|---|---:|---:|
| 进程退出码 | 0 | 0 |
| 有效历元 | 2129 | 2129 |
| 固定解 Q=1 | 466 | 466 |
| 浮点解 Q=2 | 1663 | 1663 |
| 固定率 | 21.89% | 21.89% |

2129 个历元中仅 2 个历元在输出末位有差异：最大纬度差 `1e-9 deg`（约 0.11 mm），最大高度差 `0.0001 m`（0.1 mm）。这是不同编译器/优化版本的浮点舍入差异，可视为数值一致。

边界测试：

- `-sys X`：退出码 2，并报告未知星座字符。
- `-f 0`：退出码 2，并报告频率数无效。
- 编译成功；3 条警告均来自未修改的 RTKLIB 2.4.3 原始源码中的 `strncpy`，本驱动无新增编译警告。

## 结论

原代码存在会直接影响解算结果的参数解析和默认配置问题。修复后，使用已有 RINEX 数据得到的历元数、解状态分布和坐标结果与 RTKPOST 参考结果一致，坐标差异不超过 0.11 mm。

